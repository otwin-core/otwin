[package]
name = "otwin-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The OTwin dynamics engine: compiled physical models, integrators, state."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"
