# Documentation build. Pinned, because a docs build that silently changes its
# own output between commits is not a build, it is a weather report.
sphinx==8.1.3
myst-parser==4.0.0
pydata-sphinx-theme==0.19.0
sphinx-copybutton==0.5.2
sphinx-design==0.6.1
linkify-it-py==2.1.0
sphinxcontrib-mermaid==1.0.0
